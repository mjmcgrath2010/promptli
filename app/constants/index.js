import { dashboardActionTypes } from './dashboard';
import { loginActionTypes } from './login';
import { adminActionTypes } from './admin';

const actionTypes = {
	DEFAULT_ACTION: 'REDUX_INIT',
};

export { actionTypes, dashboardActionTypes, loginActionTypes, adminActionTypes };
