const mongoose = require('mongoose')
const schema = mongoose.Schema

const {{properCase name}}Model = new schema({
    title: {
        type: String,
        required: true,
    } ,
    owner: {
        type : mongoose.Schema.Types.ObjectId,
        ref: 'user'
    }
})

module.exports = mongoose.model('{{camelCase name}}', {{properCase name}}Model)
