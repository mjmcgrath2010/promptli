#setup:
#  addons:
#    - plan: heroku-postgresql
#      as: DATABASE
#  config:
#    S3_BUCKET: my-example-bucket
build:
  docker:
    app: app/Dockerfile.prod
    widget: widget/Dockerfile.prod
    nginx: app/nginx/Dockerfile.prod
  config:
    RAILS_ENV: development
    FOO: bar
release:
  image: app
run:
  app: yarn start
  widget: yarn start
  nginx: nginx -g daemon off;

