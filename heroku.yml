#setup:
#  addons:
#    - plan: heroku-postgresql
#      as: DATABASE
#  config:
#    S3_BUCKET: my-example-bucket
build:
  docker:
    web: app/Dockerfile.prod
    widget: widget/Dockerfile.prod
    nginx: app/nginx/Dockerfile.prod
  config:
    NODE_ENV: production
release:
  image: web
run:
  web: yarn start
  widget: yarn start
  nginx: nginx -g daemon off;

