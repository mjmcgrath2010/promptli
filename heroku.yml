#setup:
#  addons:
#    - plan: heroku-postgresql
#      as: DATABASE
#  config:
#    S3_BUCKET: my-example-bucket
build:
  docker:
    web: Dockerfile
  config:
    NODE_ENV: production
    APP_URL: https://prompltli.app
    WIDGET_URL: https://dev.prompltli.app
release:
  image: web
run:
  web: yarn start
