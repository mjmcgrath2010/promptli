#setup:
#  addons:
#    - plan: heroku-postgresql
#      as: DATABASE
#  config:
#    S3_BUCKET: my-example-bucket
build:
  docker:
    web.1: app/Dockerfile.prod
    web.2: widget/Dockerfile.prod
    nginx: app/nginx/Dockerfile.prod
  config:
    NODE_ENV: production
release:
  image: web.1
run:
  web.1: yarn start
  web.2: yarn start
  nginx: nginx -g daemon off;

